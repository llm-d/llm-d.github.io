[build]
  command = "npm run build && cd preview && bash scripts/merge-docs-prs.sh /tmp/llmd-merged && LLMD_REPO=/tmp/llmd-merged bash scripts/sync-docs.sh main && npm install && npm run build && cd .. && cp -r preview/build build/docs"
  publish = "build"

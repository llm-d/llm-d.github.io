# Preview scripts

Transform tests live in `test-fixtures/` and run via Go (`make test-llmd-site`).

Production transforms are implemented in `tools/llmd-site/internal/transform/`.

Legacy bash transforms for golden `--legacy` baselines remain under
[`legacy/preview/scripts/transformations.sh`](../../legacy/preview/scripts/transformations.sh).
